var edad  = 21
edad += 1 //incremento
var peso  = 75
peso -= 2 //decremento

var burguer = 3

peso = peso + burguer

deporte = 5
peso = peso - deporte

var pola = 200.3
var total = Math.round(pola * 100 * 3) / 100 // total para un decimal
var totalStr = total.toFixed(3) //tiene en cuenta dos decimales despues de la coma
var total2 = parseFloat(totalStr)// vuelve flotante el STRING

var pizza = 8
var personas = 4

var cantidadDePorcionesPorPersona = pizza / personas